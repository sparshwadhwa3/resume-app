apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: resume
  name: resume
spec:
  replicas: 1
  selector:
    matchLabels:
      app: resume
  template:
    metadata:
      labels:
        app: resume
    spec:
      containers:
      - image: resumecr.azurecr.io/resumeapp:37
        name: resume
        ports:
        - containerPort: 80
          name: resume
      imagePullSecrets:
      - name: acr-secret
